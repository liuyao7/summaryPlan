# 运算符

逻辑运算符
 - & 和 && 的区别：&& 是短路逻辑，即如果第一个条件为 false，则不会判断第二个条件；而 & 是无论什么情况都会判断所有条件
 - | 和 || 的区别：|| 是短路逻辑，即如果第一个条件为 true，则不会判断第二个条件；而 | 是无论什么情况都会判断所有条件


 # 数组声明
 1. 静态初始化：数组变量的赋值与数组元素的赋值操作同时进行
 2. 动态初始化：先声明数组变量，再进行数组元素的赋值操作