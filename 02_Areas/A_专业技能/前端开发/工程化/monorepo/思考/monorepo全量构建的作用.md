一般monorepo项目会和turbo组合使用来进行全量构建，有啥用呢？
其实在项目里，它有时候就是个本地开发工具，用于：

1. 全量类型检查 pnpm check-types（turbo run check-types）— 改了一个公共类型，需要确认所有包都没报错，本地验证构建无误 — 改了一堆包，跑一下确认都能通过编译
所以结论：你的直觉是对的，这个项目 CI/CD 从来没有全量编译过，一直是按需编译目标应用。turbo run build 只是个本地辅助工具。